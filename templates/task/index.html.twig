<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Distribution</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-5">
    <h1 class="mb-4">Task Distribution</h1>

    <table class="table table-bordered">
        <thead class="table-light">
        <tr>
            <th>Developer ID</th>
            <th>Task ID</th>
            <th>Time (hours)</th>
        </tr>
        </thead>
        <tbody>
        {% for developerId, tasks in schedule.assignments %}
            {% for task in tasks %}
                <tr>
                    <td>{{ task.developer }}</td>
                    <td>{{ task.task }}</td>
                    <td>{{ task.time }}</td>
                </tr>
            {% endfor %}
        {% endfor %}
        </tbody>
    </table>

    <div class="mt-4">
        <h3>Total Week: <span class="badge bg-primary">{{ schedule.total_weeks }}</span></h3>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
